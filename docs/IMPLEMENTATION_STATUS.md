---
title: "Implementation Status"
last_updated: 2026-08-13
status: Active
owner: "TODO — assign a Documentation Owner"
---

# Implementation Status

## Purpose

A living snapshot of what's actually built in TransferX right now, **verified against the code** — not a restatement of what Linear tickets claim, and not a history of changes (see [`CHANGELOG.md`](./CHANGELOG.md) for that).

## Scope

In scope: current, verified build status by area.
Out of scope: the plan/roadmap (see [`product/roadmap.md`](./product/roadmap.md)), ticket-level detail (Linear is the source of truth for that), a chronological log of changes (see [`CHANGELOG.md`](./CHANGELOG.md)).

## Why "verified" matters here

A Linear ticket marked Done is a claim, not a guarantee — a later change can silently break an earlier ticket's promise without either ticket's status reflecting it. This file exists specifically to hold the *checked* answer, which sometimes differs from what the backlog says. If you're updating a row below, that means you looked at the actual code for that area during this session, not that you copied a ticket's status.

Maintained by the [`documentation-standards`](../.claude/skills/documentation-standards/SKILL.md) skill.

## Status by area

| Area | Status | Last verified | Notes |
|---|---|---|---|
| Documentation system (`/docs`) | Built | 2026-07-06 | This structure and its conventions. Gained a seventh area, `feature_spec/`, holding three implementation-ready build specs (see [`feature_spec/README.md`](./feature_spec/README.md)) — these are planning documents, not built product; they don't change any other row here until implemented. |
| Claude Code project skills (`.claude/skills/`) | Built | 2026-07-03 | Five skills — see [`docs/README.md`](./README.md). |
| Marketplace Core — Phase 3 (player consent) | Fixed | 2026-07-03 | A non-mandated deal skipped `PERSONAL_TERMS` entirely (TRA-60 regression). Fixed; the buying club can now propose terms and the player consents directly. Covered by `backend/tests/test_deals.py`. |
| Marketplace Core — Phase 4 (trust foundation) | Fixed | 2026-07-04 | TRA-127 (agent-negotiation hijack) and the five access-control gaps from the workflow audit — TRA-137 (deal access for agent/player), TRA-138 (seller-ownership validation), TRA-139 (public reserve-price/bid leak), TRA-140 (medical/personal-terms exposure), TRA-141 (audit-log scoping) — are all fixed and regression-tested. The audit log is now comprehensive (every deal-mutating action emits an event) and actor-attributed (`actor_user_id` was always null before this session; now populated everywhere, with labels resolved in both the JSON endpoint and CSV export). Verified boundary table: [`security-and-compliance/permissions-model.md`](./security-and-compliance/permissions-model.md). Remaining Phase 4 items: TRA-143 (player identity attestation), TRA-144 (mandate player-confirmation) — see Linear. TRA-146 shipped 2026-07-10 (see the club-team-roles row below). |
| Club team accounts, roles & onboarding (TRA-151/146/152/86 + approvals + first-run) | Built | 2026-07-10 | All six phases of [`feature_spec/club-team-roles-and-onboarding.md`](./feature_spec/club-team-roles-and-onboarding.md): capability matrix enforced on every club write route (`require_club_write_access` deleted; its 9 sites plus every previously-ungated club write the implementation-time re-grep found — offer negotiation messages, squad edits, player/contract creation — now gated); staff deal access with the D4 regression test pinning "READONLY staff + deal visibility ⇒ 403 on every deal write"; role-mapped notification routing (D5); owner-run team management with sha256-hashed single-use invitations + public `/accept-invite`; spending-approval thresholds (202 capture → owner/SD decision → re-validated execution, `APPROVED_FAILED` on stale state, daily expiry job); per-persona first-run checklists (frontend-only, zero new endpoints). Migrations `0049`–`0051` written but **not yet applied to the dev DB**, and the spec's demo script has not been driven against the live stack — the backend entrypoint applies migrations on next `docker compose up`. Verified by 57 new backend tests (full suite: 362 passed) + TypeScript clean + frontend suite unchanged (same 23 pre-existing failures, 96 passing). Deviations recorded in the spec. |
| Marketplace Core — Phase 2 (deal structure) | Fixed (commission) | 2026-07-04 | Commission was only tracked if the agent manually entered an absolute amount alongside the percentage — no `AgentCommission` record was created otherwise, silently. Now auto-derives the amount from `commission_pct × agreed_fee` whenever only a percentage is given. Rest of Phase 2 (loans, add-ons, sell-on, instalments) not independently re-verified this session. |
| Marketplace Core — Phase 3 (player consent) | Reworked | 2026-07-04 | Personal terms used to be captured twice — once (informally) during `AGENT_NEGOTIATION`, again at `PERSONAL_TERMS` — with no reconciliation between the two consent statuses. Per [ADR 0002](./product/decisions/0002-single-capture-point-for-personal-terms.md), now captured exactly once, at `PERSONAL_TERMS`, for both mandated and non-mandated deals, with one consistent account-gated proxy rule (real player consents if they have an account; mandated agent proxies only if they don't). |
| Marketplace Core — Medical check UI (TRA-61) | Built | 2026-07-05 | The backend (`PUT /deals/{id}/medical-check`, staff-only) was always fully functional but had no UI anywhere. `DealDetailPage` now shows a Medical Check panel — status/notes visible to every deal participant, with a staff-only inline control to set or update it and a note when a `FAILED` status is blocking `PAPERWORK → CONFIRMED`. Verified via a live API smoke test (login, PUT, GET round-trip) against the running dev stack, not just type-checking. |
| Marketplace Core — Phase 0–2 (finance, actors) | TODO | — | Not independently re-verified this session (commission handling above is the one Phase 2 exception); see [`product/roadmap.md`](./product/roadmap.md). |
| Differentiation — Fair-value-vs-asking signal (TRA-91/TRA-92) | Built | 2026-08-09 | Backend `valuation` module (deterministic `boxscore-v1` model, append-only history, `/valuation` endpoints, sale-detail embed, daily job) and UI. Verified by 31 tests (403 total passing) plus a live recompute 2026-08-09: 2,359 players covered (29.8%), confidence distribution 948 HIGH / 814 MEDIUM / 597 LOW — previously zero HIGH. The 12k vendor-sync stat snapshots now feed the model on every restart. Built per [`feature_spec/fair-value-vs-asking-signal.md`](./feature_spec/fair-value-vs-asking-signal.md). |
| Agent Experience — Milestone 3 (manage deals end to end) | Fixed | 2026-07-04 | Several real bugs found by manually driving the flow, not caught by Linear's "shipped" status: an invited agent couldn't open the deal room before their first negotiation write (deadlock); the agent couldn't trigger `AGENT_NEGOTIATION → PERSONAL_TERMS` (club-only bug); a club couldn't advance `PERSONAL_TERMS → PAPERWORK` either (frontend gating bug); `set_personal_terms` accepted any agent, not just the mandated one; agent invitations kept showing for already-collapsed/completed deals. All fixed and regression-tested. |
| Agent Experience — Milestones 1, 2, 4, 5 | TODO | — | Not independently re-verified this session. |
| Differentiation & Demo Readiness — Deal-stage demo coverage | Built | 2026-08-09 | `scripts/seed_demo.py` generates six scenarios covering every lifecycle stage from existing clubs/players/mandates (Ødegaard, Saka, Caicedo, Gravenberch etc.) via the real service layer — no hand-inserted deals. Combined with three pre-existing `COMPLETED` deals, all seven stages are demonstrable. `--reset` restores the exact pre-run state, including fully reversing a transfer executed mid-demo. Deterministic by seed. See [`feature_spec/demo-scenario-generator.md`](./feature_spec/demo-scenario-generator.md). Reference snapshot committed at `backend/seeds/transferx_reference_20260808.dump` (7.2 MB, 85 tables). |
| Differentiation & Demo Readiness — Demo-readiness audit | Delivered | 2026-08-11 | 23 verified findings (5 critical / 7 high / 11 medium) in [`DEMO_READINESS_AUDIT.md`](./DEMO_READINESS_AUDIT.md). C1–C5 resolved; remaining priorities in the audit's recommendation table. **C5 was added 2026-08-11**, found by following up a user question about one player rather than by the audit sweep: `PlayerStatus` had no way to express "contracted to a club that isn't on TransferX", so 7,825 of 7,914 players stored as `FREE_AGENT` — displayed in the green "available" badge, and signable for £0 via `create_free_agent_deal`, which gated on that status alone. Fixed per [ADR 0003](./architecture/decisions/0003-player-status-distinguishes-external-clubs.md). Worth noting for future audits: read-path spot-checks could not have caught this — four separate display-layer overrides were already masking it, while the write path stayed open. |
| Production & Business Readiness — Vendor sync reliability | Fixed | 2026-08-08 | Vendor player-stats sync (API-Football) was silently broken end-to-end — a naive/aware datetime bug crashed the bookkeeping write on every attempt, so nothing had ever synced successfully. Fixed, plus added per-run history (`vendor_sync_runs`) with full params/result/error breakdown on `/admin/vendor`, and closed a latent bug where sync could overwrite `team_name` for a player under an active TransferX contract (ownership itself — `current_club_id`/`Contract` — was never at risk; see [ADR 0001](./architecture/decisions/0001-vendor-data-never-overrides-transferx-contract.md)). Verified live: all 11 platform-tracked leagues (`world_leagues`) synced for season 2025 — 595 API requests, ~149 new players, ~12k stat snapshots, 0 errors. |
| Production & Business Readiness — Railway environment backfilled | Built | 2026-08-10 | Railway's database was live but materially behind local: only 5 of 11 leagues, no valuations, zero agent accounts. Fixed the migration chain first (Railway had `0059`–`0061` applied from a remote branch that was force-push-overwritten locally; recreated matching placeholder migrations, renumbered the local `vendor_sync_runs` migration to `0062`). Then synced all 11 leagues against Railway's public DB endpoint (~4,458 new players, ~7,351 updated, ~12k snapshots, 0 errors) and recomputed valuations the same way (`backend/scripts/recompute_valuations_railway.py`, run locally rather than restarting the live service) — 2,354 valued, confidence distribution matching local almost exactly (948 HIGH / 812 MEDIUM / 594 LOW). Created 3 agent accounts with 6 mandates via the real API (register → login → `POST /mandates/`), including one on Saka, mirroring local dev's agent identities. |
| Production & Business Readiness | TODO | — | Known accurate as of the last check: no production environment is configured yet (see [`operations/environments-and-deployment.md`](./operations/environments-and-deployment.md)). |
| UI Redesign — Phases 0–12 of 13 (all frontend phases) | Built, not merged | 2026-08-11 | Full frontend visual redesign on branch `redesign/ui-light-theme` (not merged to main — nothing here is live). Phase 0: branch + baseline (backend 403/26, frontend 96 passed/23 pre-existing failures, recorded in [`design_handoff_transferx/BASELINE.md`](./design_handoff_transferx/BASELINE.md)). Phase 1: light+dark token layer + `ThemeContext`. Phase 2: full primitive set restyled/built (`ResponsiveTable`, `Modal`, `Input`, `Select`, `Tabs`, `Tooltip`, `Alert`, etc.). Phases 3–11: shell/nav, all three dashboard variants, offers, deal room, finance/approvals, squad, browse players, sale detail, and the remaining ~41 pages. Phase 12 (final): every route walked at 375/768/1024/1280px, contrast/keyboard/focus-trap audit, 200% zoom check — 22 real overflow issues found and root-caused (not surface-patched) down to 0. Verified throughout against a rebuilt frontend container (serves a production `vite build`, not a live dev server) with Playwright — real login, real data, screenshots, zero console errors; both suites stayed at or above the Phase 0 baseline the whole way (final: same 16 of the original 23 frontend failures, a subset — the other 7 were fixed; backend unaffected, no backend changes in this track). Full 13-phase plan and per-phase detail: `C:\Users\aashi\.claude\plans\fizzy-pondering-thacker.md` (local to this machine, not in the repo). Not yet merged to `main`, and the parallel backend track's frontend-consuming half is not yet wired up — see the backend-track row below. |
| UI Redesign — Backend track (B1–B6 of B1–B7) | Built | 2026-08-11 | The parallel backend track `SESSIONS.md` calls for, which the frontend phases above shipped against as documented client-side fallbacks (`lib/whoseMove.ts`, batch valuation fetches, a self-commented-as-incomplete `CommitmentsTable`, ad-hoc client contract-cliff bucketing). B7 (present-value effect) stays deferred per `DECISIONS.md` item 5's own default. **B1**: `whose_move` field (your/their/neither) added to `OfferResponse`/`DealResponse`/`SaleResponse`, ported from `lib/whoseMove.ts` exactly — offers/sales used existing columns, deals use the same `updated_at`-based 72h proxy the frontend already ships (documented as an accepted approximation, not a new schema field). **B2**: new `dashboard` module, `GET /clubs/me/dashboard` — ranked "waiting on you" aggregate across offers/deals/sales/approvals, replacing the frontend's 5-query client concat; agent side already had a server equivalent (`action_required`) so no parallel build there. **B5**: new `GET /clubs/me/commitments` — row-level attribution of `ClubFinance`'s reserved/committed totals (offers, active bids, in-progress deals, transfer + wage), replacing a client reconstruction whose own code comment admitted it missed sale bids and all wage commitments. **B6**: new `GET /clubs/me/contract-cliff` (kept separate from the existing `/clubs/me/expiring-contracts`, whose bare-array shape `DashboardPage.tsx` already depends on) — windowed (Under 6mo/6–12mo/12–24mo) expiry aggregation with value-at-risk, using the fair-value model where a player has one and falling back to the legacy `market_value` field otherwise (~30% model coverage today). **B3**: `/players/market` gained a `sort_by=value` param (fair-value minus nominal market_value, server-side, via a window-function join) — the existing "Best value first" UI chip was page-local-only because this didn't exist. **B4**: small `wage_fit` block (`fits`, `wage_room_after`) added to player market/detail responses for authenticated club viewers, from data that was already fully exposed. Zero database migrations — every item is additive computation/aggregation over existing columns. Two pre-existing bugs found and fixed as a side effect (not part of any B-item's scope): a datetime tzinfo-comparison crash in `sales/service.py`'s bid-deadline check, same class as several already-guarded spots elsewhere in the codebase (SQLite drops tzinfo; Postgres doesn't, so this never surfaced in production). Verified by 26 new backend tests (full suite: 429 passed, up from the 403 baseline); `ruff` clean on every touched/new file. Not yet consumed by the frontend — that's a separate follow-up, out of this track's scope by design (`SESSIONS.md`: "parallel, independent of the frontend sessions"). |

| UI Redesign — merged to `main` | Done | 2026-08-13 | The whole redesign branch is now on `main` and pushed (`origin/main`). Merged as PRs #1 and #2 in 7 commits: redesign Phases 4–12, backend track B1–B6, ADR 0003, the theme control, the terminal-state UX fixes, and the Railway verification correction. `redesign/ui-light-theme` is a full ancestor of `main` — no divergence. Railway is deployed and at migration `0064`. |
| Offers — anonymous buying club | Built | 2026-08-13 | A buyer can approach without disclosing who they are; the seller sees `A {league} club` plus an explicit Anonymous marker, and the buyer is revealed on acceptance only (rejection/withdrawal/expiry leave them permanently undisclosed). Opt-in per offer (`offers.is_anonymous`, migration `0064`), not per club. Masking is server-side in `_offer_response` — the one function every offer response passes through — because identity leaks through five fields, four of them ids that resolve via `GET /clubs/{id}`, not just the club name. The competition order book masks anonymous rivals too. Verified live: the buyer's club id appears nowhere in the seller's payload. 6 new tests, two asserting absence of the id from the whole serialised body; confirmed they fail with the mask disabled. Rationale and rejected alternatives: [ADR 0004](./architecture/decisions/0004-anonymous-buyer-masked-server-side.md). **Auction bids are not covered** — separate model, separate order book, unchanged. |
| Offers — inbox, sidebar signals, terminal-state honesty | Built | 2026-08-13 | Offer Inbox groups competing offers one row per player (`2 clubs` chip, best fee, opens the best offer's order book) and splits the old "Everything else" bucket into **In play** / **Closed**. Sidebar carries "waiting on you" count badges on Inbox/Deals/Auctions/Approvals — the first consumer of B2, one request feeding every badge; club accounts only (agents make zero calls, verified). The War Room's Tier 1 band is now server-derived from the same B2 aggregate sharing one query key, so badge and band cannot disagree. Separately, resolved listings and accepted offers now report their outcome rather than a stale-but-true status (a collapsed deal no longer renders green "Accepted"). Also fixed: a 500 on any fee-less offer (`TypeError` formatting `None`), and a fifth site showing external-club players as "Free agent". |
| Test suites — current totals | Verified | 2026-08-13 | **Backend 447 passed, 0 failed.** **Frontend 111 passed, 16 failed** — the 16 are the long-standing stale-fixture failures tracked as audit [M11](./DEMO_READINESS_AUDIT.md), not regressions; they are a subset of the original 23 recorded in `design_handoff_transferx/BASELINE.md`. Earlier rows in this table quote the totals that were true at *their* verification date (e.g. 429 on 2026-08-11) — this row is the current one. One flake observed: `test_bid_count_and_best_bid_hidden_from_non_seller` failed once in a full run and passed in isolation and on a clean re-run; unrelated to that session's changes, not investigated. |
| Frontend type checking | **Broken, then corrected** | 2026-08-13 | `npx tsc --noEmit` — the command used to verify types all through this project — resolves a project-references stub with `"files": []` and checks **nothing**, always exiting 0. The real build (`tsc -b && vite build`) was never fooled, so shipped bundles are unaffected, but every "tsc clean" claim in prior session summaries and commit messages is void as evidence. Correct invocation is `npx tsc -b --noEmit`, which reports **47 pre-existing errors** — confirmed pre-existing by stashing all working changes and re-running against clean history. Roughly 14 unused declarations, ~6 test-fixture drift, and ~25 genuine type mismatches in app code (e.g. `AdminPlayerDetailPage` reading a `Player.contracts` that does not exist). Not triaged — logged as audit [H9](./DEMO_READINESS_AUDIT.md). All work committed on 2026-08-13 was verified against the 47 baseline and added zero new errors. |

> **TODO:** This table is intentionally sparse on first creation rather than guessed at wholesale. Fill in a row properly (with a real verification date) the next time a session does substantive work in that area — don't backfill the rest from memory in one sitting, since that reintroduces the exact "unverified claim" problem this document exists to avoid.

## Related documents

- [`CHANGELOG.md`](./CHANGELOG.md) — the history of changes, as distinct from this current-state snapshot
- [`PRODUCT_SPEC.md`](./PRODUCT_SPEC.md) — master index and high-level current-state table
- [`product/roadmap.md`](./product/roadmap.md) — the plan this status is measured against
